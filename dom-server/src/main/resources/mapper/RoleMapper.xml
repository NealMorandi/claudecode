<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dom.dormitory.mapper.RoleMapper">

    <resultMap id="BaseResultMap" type="com.dom.dormitory.entity.Role">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="display_name" property="displayName"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT id, name, display_name, created_at
        FROM roles
        ORDER BY id
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, name, display_name, created_at
        FROM roles
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO roles (name, display_name, created_at)
        VALUES (#{name}, #{displayName}, NOW())
    </insert>

    <update id="update">
        UPDATE roles
        SET name = #{name}, display_name = #{displayName}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM roles WHERE id = #{id}
    </delete>

</mapper>
